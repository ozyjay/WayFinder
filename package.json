{
  "name": "wayfinder",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "compile": "npm run compile --workspace=@wayfinder/extension",
    "test": "npm run test --workspace=@wayfinder/extension",
    "eval:readback": "npm run eval:readback --workspace=@wayfinder/extension"
  }
}
